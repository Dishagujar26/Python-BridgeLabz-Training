# Can we have a set with 18 (int) and '18' (str) as a value in it? 
# Yes, we can have a set with both 18 (int) and '18' (str) as values in it because sets can contain elements of different data types.

my_set = {18, '18'}
print(my_set)  # Output: {18, '18'}
