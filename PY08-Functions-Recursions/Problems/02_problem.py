
temp_in_celsius = float(input("Enter temperature in Celsius: "))
def celsius_to_fahrenheit(celsius):
    return (celsius * 9/5) + 32

print("Temperature in Fahrenheit:", celsius_to_fahrenheit(temp_in_celsius))